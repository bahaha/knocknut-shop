spring:
  application.name: auth-center
  profiles.active: dev
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.flyway.FlywayAutoConfiguration

server.port: 0

knocknut:
  data-access:
    mysql: jpa